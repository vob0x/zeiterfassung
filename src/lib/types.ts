// ============================================================================
// Re-export from main types.ts to avoid duplication
// ============================================================================

// NOTE: Primary type definitions are in src/types.ts
// This file should use those definitions instead of duplicating them
// Only type aliases and utilities specific to lib should be here

export type Language = 'de' | 'fr' | 'en';
export type Theme = 'cyber' | 'light';
